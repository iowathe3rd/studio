# 📋 System Prompts в Проекте

## 🗂️ Где находятся системные промпты

### Основной файл с промптами
**Путь:** `lib/ai/prompts.ts`

Этот файл содержит все основные системные промпты и конфигурации.

## 📝 Все системные промпты

### 1. 🌍 Regular Prompt (Основной промпт)
**Место:** `lib/ai/prompts.ts` - строка 41-42  
**Использование:** Базовый промпт для всех чатов  
**Тип:** Строка

```typescript
export const regularPrompt =
  "You are a friendly assistant! Keep your responses concise and helpful.";
```

**Что он делает:**
- Задает основной тон AI ассистента
- Заставляет быть дружелюбным
- Требует кратких и полезных ответов

---

### 2. 📄 Artifacts Prompt (Промпт для документов)
**Место:** `lib/ai/prompts.ts` - строки 4-39  
**Использование:** Инструкции для работы с документами и кодом  
**Тип:** Строка (большой блок)

```typescript
export const artifactsPrompt = `
Artifacts is a special user interface mode...
```

**Ключевые правила:**
- ✅ Используй artifacts для кода (>10 строк)
- ✅ Используй artifacts для контента для переиспользования
- ❌ НЕ обновляй документы сразу после создания
- ❌ НЕ вызывай один инструмент дважды за раз

**Применяется для:**
- `createDocument` tool
- `updateDocument` tool
- Кодовых фрагментов
- Контента, который нужно сохранить

---

### 3. 🐍 Code Prompt (Промпт для кода)
**Место:** `lib/ai/prompts.ts` - строки 75-99  
**Использование:** Специальные инструкции для Python кода  
**Тип:** Строка

```typescript
export const codePrompt = `
You are a Python code generator...
```

**Правила для кода:**
1. Каждый фрагмент должен быть self-contained
2. Используй print() для вывода
3. Кратко (<15 строк)
4. Только стандартная библиотека
5. Graceful error handling
6. Нет interactive функций (input())
7. Нет файловых операций
8. Нет бесконечных циклов

**Примеры в промпте:**
- Факториал
- Другие полезные примеры

---

### 4. 📊 Sheet Prompt (Промпт для таблиц)
**Место:** `lib/ai/prompts.ts` - строки 101-103  
**Использование:** Создание CSV таблиц  
**Тип:** Строка

```typescript
export const sheetPrompt = `
You are a spreadsheet creation assistant. 
Create a spreadsheet in csv format...
`;
```

**Что делает:**
- Помогает создавать CSV таблицы
- Добавляет осмысленные заголовки
- Генерирует релевантные данные

---

### 5. ✏️ Update Document Prompt (Промпт обновления документа)
**Место:** `lib/ai/prompts.ts` - строки 105-120  
**Использование:** Динамический промпт для обновления документов  
**Тип:** Функция (функция, возвращающая строку)

```typescript
export const updateDocumentPrompt = (
  currentContent: string | null,
  type: ArtifactKind
) => {
  // Возвращает промпт с текущим контентом
};
```

**Параметры:**
- `currentContent` - текущее содержимое документа
- `type` - тип документа (code, sheet, text)

**Применяется для:**
- Улучшения документов
- Указания на что обновлять
- Контекстного улучшения

---

### 6. 🎯 Title Prompt (Промпт для заголовков)
**Место:** `lib/ai/prompts.ts` - строки 122-126  
**Использование:** Генерация заголовков чатов  
**Тип:** Строка

```typescript
export const titlePrompt = `
  - you will generate a short title based on the first message...
  - ensure it is not more than 80 characters long
  - the title should be a summary of the user's message
  - do not use quotes or colons
`;
```

**Правила:**
- Макс 80 символов
- Резюме первого сообщения
- Без кавычек и двоеточий

---

### 7. 🎓 System Prompt (Главный системный промпт)
**Место:** `lib/ai/prompts.ts` - строки 59-73  
**Использование:** Основной промпт для всех разговоров  
**Тип:** Функция (возвращает комбинированный промпт)

```typescript
export const systemPrompt = ({
  selectedChatModel,
  requestHints,
}: {
  selectedChatModel: string;
  requestHints: RequestHints;
}) => {
  // Комбинирует регулярный + локационный + artifacts промпты
};
```

**Логика:**
- Для `chat-model-reasoning`: regularPrompt + requestPrompt
- Для других моделей: regularPrompt + requestPrompt + artifactsPrompt

**Параметры:**
- `selectedChatModel` - выбранная модель
- `requestHints` - геолокационные данные пользователя

---

### 8. 📍 Request Hints Prompt (Геолокационный контекст)
**Место:** `lib/ai/prompts.ts` - строки 51-57  
**Использование:** Добавление локационного контекста  
**Тип:** Функция (возвращает строку с геоданными)

```typescript
export const getRequestPromptFromHints = (requestHints: RequestHints) => `
About the origin of user's request:
- lat: ${requestHints.latitude}
- lon: ${requestHints.longitude}
- city: ${requestHints.city}
- country: ${requestHints.country}
`;
```

**Информация:**
- Широта
- Долгота
- Город
- Страна

**Используется:**
- В основном systemPrompt
- Дает контекст о местоположении пользователя

---

### 9. 💡 Suggestions Prompt (Промпт для предложений)
**Место:** `lib/ai/tools/request-suggestions.ts` - строки 54-55  
**Использование:** Генерация редакторских предложений  
**Тип:** Встроенная строка

```typescript
system: "You are a help writing assistant. Given a piece of writing, please offer suggestions to improve the piece of writing and describe the change. It is very important for the edits to contain full sentences instead of just words. Max 5 suggestions."
```

**Правила:**
- Помощник по писательству
- Предлагает улучшения
- Максимум 5 предложений
- Полные предложения (не слова)

---

## 🔄 Как используются промпты

### В API чата
**Файл:** `app/(chat)/api/chat/route.ts` - строка 217

```typescript
const result = streamText({
  model,
  system: systemPrompt({ selectedChatModel, requestHints }),
  messages: convertToModelMessages(sanitizedMessages),
  // ...
});
```

**Процесс:**
1. Выбирается модель
2. Генерируется systemPrompt с учетом модели и геолокации
3. Передается в streamText для генерации ответа

### Для генерации заголовков
**Файл:** `app/(chat)/actions.ts`

```typescript
const { text: title } = await generateText({
  model,
  system: titlePrompt,
  prompt: getTextFromMessage(message),
});
```

### Для предложений по редактированию
**Файл:** `lib/ai/tools/request-suggestions.ts`

```typescript
const { elementStream } = streamObject({
  model,
  system: "You are a help writing assistant...",
  prompt: document.content,
  // ...
});
```

---

## 📊 Таблица использования

| Промпт | Файл | Строки | Модель | Частота |
|--------|------|--------|--------|---------|
| regularPrompt | prompts.ts | 41-42 | Все | Всегда |
| artifactsPrompt | prompts.ts | 4-39 | Не reasoning | Всегда |
| codePrompt | prompts.ts | 75-99 | (ref) | По запросу |
| sheetPrompt | prompts.ts | 101-103 | (ref) | По запросу |
| updateDocumentPrompt | prompts.ts | 105-120 | artifact | По запросу |
| titlePrompt | prompts.ts | 122-126 | title-model | После каждого чата |
| systemPrompt | prompts.ts | 59-73 | Все | Для каждого запроса |
| requestHintsPrompt | prompts.ts | 51-57 | Все | Для каждого запроса |
| suggestionsPrompt | request-suggestions.ts | 54-55 | artifact | По запросу tool |

---

## 🎯 Модели и их промпты

### chat-model-reasoning
```
systemPrompt использует:
- regularPrompt
- requestHintsPrompt
- БЕЗ artifactsPrompt
```

### Остальные модели (chat-model-fast, etc.)
```
systemPrompt использует:
- regularPrompt
- requestHintsPrompt
- artifactsPrompt
```

### title-model
```
Использует:
- titlePrompt
```

### artifact-model
```
Использует:
- Встроенные промпты для конкретных задач
- updateDocumentPrompt
- suggestionsPrompt
```

---

## 🔍 Поиск промптов в коде

### Все промпты определены в одном файле:
```bash
lib/ai/prompts.ts
```

### Использование в проекте:
```bash
# Chat API
app/(chat)/api/chat/route.ts - строка 217

# Title generation
app/(chat)/actions.ts

# Suggestions
lib/ai/tools/request-suggestions.ts - строка 54-55
```

---

## 📝 Типы данных для промптов

### RequestHints
```typescript
type RequestHints = {
  latitude: number;
  longitude: number;
  city: string;
  country: string;
};
```

### ArtifactKind
```typescript
type ArtifactKind = 'code' | 'sheet' | 'text';
```

---

## 🚀 Как добавить новый промпт

1. **Добавь в `lib/ai/prompts.ts`:**
```typescript
export const myNewPrompt = `Your prompt text here`;
```

2. **Импортируй где нужно:**
```typescript
import { myNewPrompt } from "@/lib/ai/prompts";
```

3. **Используй в коде:**
```typescript
const result = streamText({
  model,
  system: myNewPrompt,
  // ...
});
```

---

## 🎓 Лучшие практики промптов

✅ **DO:**
- Четкие инструкции
- Примеры
- Ограничения (макс 5 items, <15 lines)
- Правила о чем NOT делать

❌ **DON'T:**
- Слишком много информации
- Противоречивые инструкции
- Без примеров для сложных задач

---

## 📖 Ссылки

- **Основной файл:** `lib/ai/prompts.ts`
- **Chat API:** `app/(chat)/api/chat/route.ts`
- **Actions:** `app/(chat)/actions.ts`
- **Tools:** `lib/ai/tools/`

---

**Последнее обновление:** 2024-11-06  
**Статус:** ✅ Полная документация
