# =============================================================================
# AI PROVIDER CONFIGURATION
# =============================================================================
# Optional: pick which provider powers default chat models (openai | gemini)
AI_DEFAULT_PROVIDER=openai

# OpenAI configuration (required if AI_DEFAULT_PROVIDER=openai)
OPENAI_API_KEY=****

# Google Gemini configuration (required if AI_DEFAULT_PROVIDER=gemini)
GEMINI_API_KEY=****

# =============================================================================
# SUPABASE CONFIGURATION
# =============================================================================
# Get these from your Supabase project settings: https://supabase.com/dashboard/project/_/settings/api

# Public variables (safe to expose in browser)
NEXT_PUBLIC_SUPABASE_URL=****
NEXT_PUBLIC_SUPABASE_ANON_KEY=****

# Server-only secrets (NEVER expose to browser)
SUPABASE_SERVICE_ROLE_KEY=****

# =============================================================================
# OPTIONAL CONFIGURATION
# =============================================================================
# Redis for resumable streams (optional, enables stream recovery)
# Instructions: https://vercel.com/docs/storage/vercel-kv
REDIS_URL=****

# Application URL (optional, defaults to http://localhost:3000 in dev)
# Used for OAuth redirects and email confirmations
NEXT_PUBLIC_APP_URL=****
